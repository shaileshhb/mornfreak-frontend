# Base URL of the Mornfreak backend API.
# The backend must list this app's origin in CORS_ORIGIN and APP_URL,
# otherwise auth cookies and email links will not work.
NEXT_PUBLIC_API_URL=http://localhost:3001
STOREFRONT_API=public_access_token
SHOPIFY_STOREFRONT_PRIVATE_TOKEN=private_access_token